spring:
  application:
    name: cultivation-help-app

  datasource:
    url: jdbc:postgresql://localhost:5433/cultivation_db1
    username: postgres
    password: 132435

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

logging:
  level:
    com.cultivation: DEBUG